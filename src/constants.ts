import { Watcher } from './types';

export const APP_SWITCHER_NAME = 'App Switcher';

export const WINDOW_WIDTH = 1280;
export const WINDOW_HEIGHT = 800;
export const WatcherTypes = Watcher.Types; // tslint:disable-line
